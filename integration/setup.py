from setuptools import setup, find_packages

setup(name="testpkg",
      version="0.0.1",
      description="A minimal Python package for testing PyPI uploads",
      long_description="A minimal Python package for testing PyPI uploads",
      long_description_content_type="text/markdown",
      packages=find_packages())
